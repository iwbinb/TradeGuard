# Non-secret defaults only. Live trading is disabled until explicitly enabled.
APP_ENV=development
FACTORY_ADDRESS=
EXECUTION_ENABLED=false
MODEL_ENDPOINT=
MODEL_NAME=
# Configure EXECUTOR_SEED and MODEL_API_KEY through a local ignored .dev.vars
# or Cloudflare's secret store. Never place credentials in VITE_* variables.
